apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: promptic-previews-wildcard
  namespace: promptic-core
spec:
  secretName: promptic-previews-tls
  issuerRef:
    name: letsencrypt-prod-dns
    kind: ClusterIssuer
  dnsNames:
    - "*.previews.promptic.site"